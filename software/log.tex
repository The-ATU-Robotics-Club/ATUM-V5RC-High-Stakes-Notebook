\chapter{Log}
\subsection{9/19/2024}
Braden met with Brandon, Preston, Everett, and Bo for an initial programming workshop. They went over the basics of programming in VEX and had a general overview of the software from the previous game. Progress on software will be slower initially, due to attention being directed toward other pressing affairs in the club (shirts, sponsors, events, and so on). 

\subsection{10/10/2024}
Today Braden got with Brandon, Preston, Everett, and Bo to go over programming the robots. They removed the old implementations for the robots from the previous year and began the basic set up for this year. Braden will remove defunct code, refactor existing code, and produce documentation prior to next meeting to make the transition easier.

\subsection{10/13/2024}
Braden upgraded the project to PROS 4.1 and our version of LVGL to 8.3.4 and made changes to the project to allow compilation. 
This involved mostly changes like
\begin{lstlisting}[language=C++]
    void setBrakeMode(const pros::motor_brake_mode_e brakeMode);
\end{lstlisting}
becoming
\begin{lstlisting}[language=C++]
    void setBrakeMode(const pros::motor_brake_mode_e brakeMode);
\end{lstlisting}
and similar adjustments. The Piston class was also removed, since an analogous class was provided with the update. 

\subsection{10/16/2024}
Braden over the last several days has been making several changes to prepare for a new GUI and logging system. Central to this is the autonomous routine selector. Last year, in order to add on another routine, change its name, or remove it, one would have to change several different areas of code in two different files. As such, it was rather inconvenient. The goal for this year, is to be able to place all routine information in one area.

To this end, we think that a system employing preprocessor macros and the use of curiously recurring template pattern (CRTP) \cite{bib:crtp} would accomplish the goal of centralizing the definition of routines. Though, this would mean that we couldn't rely on polymorphism to dynamically setup the robot at the start of the program. 

Thus, Braden has added some lines to the default makefile provided by PROS to add a preprocessor definition into the program representing the unique ID of the brain plugged in. A script was also added to force recompilation upon the ID changing. 
\begin{lstlisting}
# Gets the unique ID for the currently wired brain, or zero otherwise.
# This is accessible in code as the preprocessor definition BRAIN_ID. 
BRAIN_ID:=$(shell pros v5 status | grep -ohZ "0x[0-9]*" || echo 0)
$(info Brain ID is $(BRAIN_ID))
$(shell ./run-brain-id-check.sh $(BRAIN_ID)) # Forces main to rebuild if brain-id has changed. 
CPPFLAGS=-D_POSIX_THREADS -D_UNIX98_THREAD_MUTEX_ATTRIBUTES -D_POSIX_TIMERS -D_POSIX_MONOTONIC_CLOCK -DBRAIN_ID=$(BRAIN_ID)
\end{lstlisting}
\subsection{10/24/2024}
Braden discussed some C++ concepts with Preston and Bo, including pointers, references, and immutability. Next week we will go over smart pointers and polymorphism. Aside from that we made a preliminary list of tasks that can be worked on in tandem with the rewrite/refactoring process:
\begin{itemize}
    \item A specialized version of a motor group, a "balanced" motor group that takes in a list of left motors and a list of right motors. If this object detects that a motor has disconnected or failed on one side, it will disable a motor on the other side.
    \item A wrapper around the current implementation for the GPS for ease of use and to properly test the sensor. 
    \item A system using the line trackers to update the position of the robot upon seeing a line, as a free "soft" reset. 
\end{itemize}
\subsection{10/28/2024}
Braden began working on the visual for the GUI and finished the main menu layout. Braden also began working on the routine selector screen. Instead of working with a physical VEX brain, we are currently making use of an LVGL simulator \cite{bib:lvglsim} to make the process go by faster.  It may seem pedantic to spend this much time on the GUI, but it will be an invaluable tool later when tuning and debugging. To see the current version of the main menu, see \ref{fig:10/28/2024 gui main menu}.
\begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{images/october/main menu gui.png}
    \caption{GUI main menu from simulator.}
    \label{fig:10/28/2024 gui main menu}
\end{figure}
\subsection{10/30/2024}
Braden finished the work on the routine selector page. 

\subsection{10/31/2024}
Braden finished the work on the log screen of the GUI. At the moment it is just the visuals, but will later be connected to the logger in code. 

Braden also set up the layout for the map screen visually, but similarly without proper methods to interface with the maps data.

\subsection{11/7/2024}
Braden stylized the map screen graph as well as prepared a style for the graph screen chart. Braden also added a loading screen, which will be helpful when IMUs are initializing and the robot should not be touched. 

Structurally, Braden also moved much of the GUI code into a separate class to begin organizing it.

\subsection{11/8/2024}
Braden added getters for the routine selector page, an important step in the creation of the interface to the GUI elements. 

\subsection{11/8/2024}
Braden added a method for writing to the log screen, necessary for later connecting the screen to the loggers. 

\subsection{11/13/2024}
Braden added several methods to support adding points to various series on the graph screen, as well as changing the range of the series. 

\subsection{11/14/2024}
Braden added methods to support placing points on the map screen. Braden also ported over the code for the GUI from the LVGL simulator to the PROS project. Additionally, Braden added a home screen image and an error screen image. 

After the GUI was ported over, the rest of the project was retrofitted to support and make use of its features (at least for autonomous routine selection). 

\subsection{11/15/2024}
Braden removed several old files. Braden also connected the logger to the log screen, so now any message above Info level will be show on the screen. Braden also did some routine refactoring of the GUI code. 

\subsection{11/21/2024}
Braden did a basic rewrite of the task class in order to better name a few methods and change to only one background task (rather than the three that were supported before). Preferably, we will later have a solution that supports an arbitrary number of background tasks. 

Braden and Preston also added a wrapper around the PROS implementation for motors. This was added to seamlessly support additional error checking and customize the class for our specific uses. 

\subsection{11/22/2024}
Braden prevented the logger from logging the same message several times by storing each message that is logged. Additionally, Braden tested that logging to the SD card was working as expected. There was also an issue with the log screen: updating it was so slow it would cause lag with the controls. This was due to the LVGL object we used for the text. We used a textarea, when this seems to be intended for user input. Braden corrected this issue by changing it to a simple text object. 

Braden also began retrofitting the previously existing error checking to the new logging system (pretty much only for the odometry). 

\subsection{12/8/2024}
Braden significantly refactor the code for the Task parent class and the Robot parent class. 

The Task class can now support an arbitrary number of background tasks functions, rather than one or a limited amount. Though, technically, it is limited by the max number of tasks supported by PROS, we will never realistically reach this limit. 

The Robot class is now setup to centralize where autonomous routines are developed. Now, the name of routines and their behavior is specified in only one location and updates in the GUI and for the robot. 

The use of preprocessor macro statements were employed to make this cleaner to use. While not typically good practice, this is a specific scenario where Braden deemed it worthwhile. 

Braden also did a basic rewrite of the Remote class to support the new namespaces provided with PROS 4 and generally make the development of controls easier. 

\subsection{12/10/2024}
Braden added anti-jam code to the intake. It is based off of a timer and a "standstill" threshold. Everytime we begin intaking, the timer starts and, when it gets past its specified time, the intake begins checking if its velocity has dropped below the given threshold. If it does, it is considered jammed and briefly outtakes. 

\subsection{12/11/2024}
Braden added in color sorting to the intake. It works fairly simple, when the color sensor sees something, if it isn't our color intake until it doesn't see it and then briefly outtake. By inserting 20 rings into the intake, we have found that it successfully sorts roughly 95\% of the rings we intake. 

\subsection{12/16/2024}
Braden changed the odometry calculation to make use of exponential pose estimation. This method essentially takes into account turning dynamics during tracking to provide a more accurate estimate (see \cite{bib:controlinfrc} for further elaboration).

In addition to the arc-length estimation we perform and the odometers Collin produced, after driving haphazardly for about 3 minutes, the tracking seemed to remain accurate. We determined this using the map on the GUI. 

Braden also retrieved the robot to bring home during winter break for additional testing.

\subsection{12/17/2024}
Braden greatly refactor the GUI away from its God class form into several, modular classes. While the code remains functionally the same, this was necessary to maintain flexibility (and probably robustness, since this arrangement allows easier debugging). 